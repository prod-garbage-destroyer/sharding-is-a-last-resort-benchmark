# CockroachDB Native Distribution

Three CockroachDB nodes run as a local insecure cluster.

The benchmark connects to the SQL listener on the first node and lets Cockroach handle distribution, replication, and transaction coordination natively.

