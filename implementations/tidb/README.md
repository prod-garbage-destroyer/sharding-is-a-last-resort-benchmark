# TiDB Native Distribution

TiDB runs with the official quick-start compose topology: PD, TiKV, and TiDB services on one Docker network.

The benchmark connects to the TiDB SQL port and exercises the same logical workload as the other targets.

